name = "contacts"
version = "1.0.0"
description = "Example: exercising sift with a contacts CRUD validation boundary"

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
sift = { path = "../../" }

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
